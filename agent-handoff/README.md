# agent-handoff (ephemeral)

Round folders for Dev ↔ Runner communication. See `agents/HANDOFF_FORMAT.md`.

This entire directory is intended to be removed from git history before a final / paper submission via `scripts/purge_agent_handoff_history.sh` (or `.ps1`).
